/**
 * PlayersService — all business logic for the players module.
 */
import { createLogger } from '@core/logger.js';

const logger = createLogger('players-service');

// Business logic methods implemented in subsequent phases
export const playersService = {
  _logger: logger,
};
