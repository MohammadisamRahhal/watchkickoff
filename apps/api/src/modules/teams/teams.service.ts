/**
 * TeamsService — all business logic for the teams module.
 */
import { createLogger } from '@core/logger.js';

const logger = createLogger('teams-service');

// Business logic methods implemented in subsequent phases
export const teamsService = {
  _logger: logger,
};
