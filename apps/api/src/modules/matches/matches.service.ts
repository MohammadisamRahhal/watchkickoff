/**
 * MatchesService — all business logic for the matches module.
 */
import { createLogger } from '@core/logger.js';

const logger = createLogger('matches-service');

// Business logic methods implemented in subsequent phases
export const matchesService = {
  _logger: logger,
};
