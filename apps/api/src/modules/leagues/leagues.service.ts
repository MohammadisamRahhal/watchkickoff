/**
 * LeaguesService — all business logic for the leagues module.
 */
import { createLogger } from '@core/logger.js';

const logger = createLogger('leagues-service');

// Business logic methods implemented in subsequent phases
export const leaguesService = {
  _logger: logger,
};
