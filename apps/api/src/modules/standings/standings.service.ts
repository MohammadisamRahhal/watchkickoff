/**
 * StandingsService — all business logic for the standings module.
 */
import { createLogger } from '@core/logger.js';

const logger = createLogger('standings-service');

// Business logic methods implemented in subsequent phases
export const standingsService = {
  _logger: logger,
};
