# Migration files are generated by drizzle-kit and committed to version control.
# Run: npm run db:generate  (generates SQL from schema changes)
# Run: npm run db:migrate   (applies pending migrations to the database)
